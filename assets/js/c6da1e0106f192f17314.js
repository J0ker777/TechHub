(()=>{const API="https://techhub-quiz-api.bolotin-denis.workers.dev",KEY='techhub-auth-v1',box=document.querySelector('[data-auth-profile]');if(!box)return;const avatar=box.querySelector('[data-auth-avatar]'),name=box.querySelector('[data-auth-name]'),logout=box.querySelector('[data-auth-logout]');let current=null;function show(value){current=value;if(!value?.sessionToken){box.hidden=true;return;}name.textContent=value.leaderboardName||value.user?.displayName||'Користувач';if(value.user?.avatarUrl){avatar.src=value.user.avatarUrl;avatar.alt='Аватар '+name.textContent;avatar.hidden=false}else avatar.hidden=true;box.hidden=false;}async function validate(){let saved;try{saved=JSON.parse(localStorage.getItem(KEY)||'null')}catch{}if(!saved?.sessionToken){show(null);return;}try{const response=await fetch(API+'/auth/me',{headers:{Authorization:'Bearer '+saved.sessionToken}});if(!response.ok)throw Error();const data=await response.json();saved={...saved,expiresAt:data.expiresAt,user:data.user};localStorage.setItem(KEY,JSON.stringify(saved));show(saved);}catch{localStorage.removeItem(KEY);show(null);}}window.addEventListener('techhub-auth-changed',event=>show(event.detail));logout.addEventListener('click',async()=>{if(current?.sessionToken)try{await fetch(API+'/auth/logout',{method:'POST',headers:{Authorization:'Bearer '+current.sessionToken}})}catch{}localStorage.removeItem(KEY);show(null);if(/(?:^|\/)(?:quizzes|quiz-[^/]+|leaderboard)\.html$/.test(location.pathname))location.href='quizzes.html';});validate();})();